import React from 'react';
import { Notes } from './Notes';
import { Addnote } from './Addnote';

export const Home = () => {
    return (
        <div>

            <Notes />
        </div>
    );
};

export default Home;
